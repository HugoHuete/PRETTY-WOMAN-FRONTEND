import { EmptyState } from '../../shared/ui/screen-state';

export function DashboardPage() {
  return (
    <EmptyState
      title="Tu resumen estará aquí"
      description="Conecta el dashboard con la API para mostrar ventas, cobros, reservas, entregas e incidencias."
    />
  );
}
